@./skills/self-harness/SKILL.md

# Self-Harness for Claude Code

This repo is a portable skill package. Use `skills/self-harness/SKILL.md` as the source of truth. The host agent must do repo discovery and intent reasoning itself. Helper scripts only scaffold templates and make simulation planning, trace normalization, weakness mining, proposal reporting, and validation artifacts repeatable inside the target repo's `.self-harness/` folder.
